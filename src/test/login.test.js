import renderer from 'react-test-renderer'
import LoginComp from '../components/login';
test('snap shot testing', () => {
 
   /* const component=renderer.create(<LoginComp/>)
    let tree=component.toJSON()
    expect(tree).toMatchSnapshot()*/
  
});
